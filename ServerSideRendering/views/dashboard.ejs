<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard</title>
    <style>
        body {
            font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
            background-color: rgb(209, 222, 247);
            margin: 0;
            display: flex;
            justify-content: center;
            /* align-items: center; */
            height: 100vh;
        }

        .dashboard-navbar{
            width: 1113px;
            height: 163px;
            background-color: #3397d6;
            border-radius: 5px;
            box-shadow:inset;
        }

        .dashboard-sidebar h2 {
            color: #2a71b0;
            margin-bottom: 20px;
            text-align: center;
        }

        .dashboard-content {
            flex: 1;
            padding: 20px;
        }

        #greeting {
            color: #2a71b0;
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 20px;
        }

        .logout-button {
            background-color: #3498db;
            color: #ffffff;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
            text-transform: uppercase;
        }

        .logout-button:hover {
            background-color: #2187c6;
        }

        nav {
            background-color: #52bee7;
            overflow: hidden;
            height: 72px;
            border: 1px solid #333;
        }

        nav a {
            float: left;
            color: #e79292;
            text-align: center;
            padding: 14px 16px;
            text-decoration: none;
            padding-top: 26px;
            padding-left: 20px;
            font-weight: bold;
        }

        nav a:hover {
            background-color: #ddd;
            color: black;
        }
    </style>
</head>

<body>
    <div class="dashboard-navbar">
        <h2>Dashboard</h2>
        <nav>
            <a href="/">Home</a>
            <a href="/contact">Contact</a>
            <a href="/about">About</a>
            <a href="/todo">Todo</a>
        </nav>
    </div>
    <div class="dashboard-content">
        <%- include('component/header.ejs', username) %>
        <!-- <div id="greeting">
            Welcome,
            <br>[User's Name]
            Replace [User's Name] with the actual username from your backend
        </div> -->
        <button class="logout-button" id="logout-button">Log Out</button>
    </div>
    <script src="/script"></script>
</body>

</html>